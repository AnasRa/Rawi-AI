import { Flex } from "@chakra-ui/react";

import { useUncontrolled } from "@mantine/hooks";
import { useEffect, useState } from "react";

export function ProgressBar({ data, onChange }: any) {
  return (
      <Flex>
          dscsdc
      </Flex>
  );
}
